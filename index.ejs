<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expenses Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" integrity="sha512-Fo3rlrZj/k7ujTnHg4CGR2D7kSs0v4LLanw2qksYuRlEzO+tcaEPQogQ0KaoGN26/zrn20ImR1DfuLWnOo7aBA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>

<nav class="navbar-nav navbar-dark bg-dark text-light text-center">
    <h1>My Expenses Tracker</h1>
</nav>

<div class="container">
    <header class="row mx-4 bg-light">
        <div class="col-md-3">
            <h5>Today: $<%=dailySpend%></h5>
        </div>
        <div class="col-md-3">
            <h5>Weekly: $<%=weeklySpend%></h5>
        </div>
        <div class="col-md-3">
            <h5>Monthly: $<%=monthlySpend%></h5>
        </div>
        <div class="col-md-3">
            <h5>Yearly: $<%=yearlySpend%></h5>
        </div>
        
    </header>
        <main>
            <div class="title">
                <h3>Add Expenses</h3>
            </div>
            <div class="form-page">
                <form action="/" method="post" class="">
                    <div class="row">
                        <div class="col-md-3">
                            <label class="form-label" for="description">Descripition:</label>
                            <input type="text" class="form-control" name="description" id="" required>
                        </div>
                
                        <div class="col-md-3">
                            <label for="dateSpend" class="form-label">Date:</label>
                            <input type="date" class="form-control" name="dateSpend" id="" required>
                        </div>
                        
                        <div class="col-md-3">
                            <label for="amount" class="form-label">Amount ($):</label>
                            <input type="text" class="form-control" name="amount" autocomplete="off" required>
                        </div>
                       
                        <div class="col-md-3" id="add-button">
                            <button type="submit" class="btn btn-dark btn-block">Add <i class="fas fa-plus"></i></button>
                         </div>
                    </div>

            </div>

            </form>

        </main>

        <section class="col">
            <div class="section-title">
                <h3>Expenses Overview</h3>
            </div>
            <div class="table-responsive">
                <table class="table align-middle table-bordered text-center table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Description</th>
                            <th>Date</th>
                            <th>Amount ($)</th>
                            <th class="bg-danger">Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        <form action="/delete" method="POST">
                            <%description.forEach(function(item){%>
                                <tr>
                                    <td><%=item.description%></td>
                                    <td><%=item.date.toLocaleString("en-SG", {year: "numeric", month: "2-digit", day: "2-digit"})%></td>
                                    <td><%=item.amount%></td>
                                    <td><input type="checkbox" name="checkbox" onchange="this.form.submit()" value="<%=item._id%>"></td>
                                </tr>
                                <%})%>
                        </form>
                    </tbody>
                </table>
            </div>
        </section>

    </div>
</div>

</body>
</html>
